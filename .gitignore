# --- Node / package managers ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# npm pack で出る一時tgz（必要なら外してOK）
*.tgz

# --- Vite / Build outputs ---
dist/
dist-ssr/
.vite/
# Viteの依存プリバンドキャッシュ（場所が変わることあり）
node_modules/.vite/

# --- Tests / Coverage ---
coverage/
.nyc_output/
.eslintcache
.stylelintcache
*.tsbuildinfo

# --- E2E (必要なければ削ってOK) ---
# Cypress
cypress/screenshots/
cypress/videos/
cypress/downloads/
# Playwright
playwright-report/
test-results/
blob-report/
.playwright/

# --- Storybook / Docs (使う場合) ---
storybook-static/
out/

# --- Env files ---
# 実機密はコミットしない。サンプルは .env.example を用意
.env
.env.*
!.env.example
!.env*.example

# （必要に応じて）トークン等が入る場合は .npmrc を除外
# .npmrc

# --- Editors / IDEs ---
.vscode/
!.vscode/extensions.json   # 推奨拡張共有したい場合のみ残す
.idea/
*.iml

# --- OS junk ---
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Desktop.ini

# --- Temp files ---
*.swp
*.swo
*~
tmp/
temp/

# --- ステップカウント ---
.VSCodeCounter

# --- (オプション) Yarn Berry を使う場合は下を有効化 ---
# .yarn/*
# !.yarn/patches
# !.yarn/plugins
# !.yarn/releases
# !.yarn/sdks
# !.yarn/versions
# .pnp.*
# .pnp.loader.mjs

# --- (オプション) Nuxt を使う場合 ---
# .nuxt/
# .output/
